---
date: 2026-08-10
depth: light
parent-review: .cg-docs/reviews/2026-08-03-editorial-theme-publishing-workflow-evidence-v2-review.md
type: verification
findings:
  P3.1: fixed
  P3.2: skipped
  P3.3: fixed
---

## Review Report

**Review mode**: light (verification)
**Prior review**: `.cg-docs/reviews/2026-08-03-editorial-theme-publishing-workflow-evidence-v2-review.md`
**Files reviewed**: 2 changed paths (`scripts/evidence/tests/manifest.test.js`; `.cg-docs/reviews/2026-08-03-...-review.md` frontmatter statuses)
**Suppression policy**: P0/P1 and cross-file breakage always reported; P2/P3 suppressed only for blocks explicitly listed as `fixed` in the prior review.

### P0 — BLOCKING

No new P0 findings.

### P1 — CRITICAL

No new P1 findings.

### P2 — IMPORTANT

No new P2 findings.

### P3 — MINOR

- **[P3.1]** `cg-code-quality` `scripts/evidence/tests/manifest.test.js:264-267` — Residual `if (fs.existsSync(pdfPath))` guard in the "each cell has a non-empty print preview artifact" test is now redundant.
  **Why**: The hardened "hashes match actual file content" test now unconditionally enforces print-preview PDF existence, non-empty, and hash-match, so the guard can never be false during a CI run; its only remaining value is the `cell.printPreviewArtifact` truthiness check above it.
  **Fix**: Drop the `if (existsSync)` guard and assert non-empty unconditionally, or fold the truthiness check into the hardened hash test and delete this test.

- **[P3.3]** `cg-code-quality` `scripts/evidence/tests/manifest.test.js:107` — Test name "manifest exists (SKIP if not yet captured)" is stale.
  **Why**: The test asserts a failure when the manifest is absent; it never skips. Since the suite is CI-gated (P2.6 fixed) with CI-generated evidence (P1.2 fixed), the "SKIP" wording misleads maintainers into thinking a local run without captured evidence is tolerated.
  **Fix**: Rename the test to describe actual behavior, e.g. "manifest exists (evidence generated by CI capture)".

### Suppressed (fixed-finding scope)

- **[P3.2]** `cg-code-quality` `scripts/evidence/tests/manifest.test.js:196-247` — Repeated 3-line exist + non-empty assertion pattern across source/view/PDF/screenshot. **Suppressed**: targets the `hashes match actual file content` block explicitly refactored by fixed finding P1.3; a fix-consequence DRY nit, not a defect. (If desired, extract a small `assertNonEmpty(filePath, label)` helper next to `sha256`.)

### Confirmed Convergent

- **[P1.3]** `manifest.test.js` hash tests — Confirmed correctly implemented and self-consistent:
  - Source/view hash checks no longer skip on missing files; they assert existence (friendly message, not raw `ENOENT`), non-empty, and byte-level SHA-256 match.
  - PDF and every viewport screenshot gain non-empty assertions alongside existing existence checks.
  - `capture.js` shares a byte-identical `sha256` helper, fails on missing artifacts before writing the manifest, and writes the manifest once at the end — so the hardened assertions encode a real producer invariant (a capture with missing referenced files cannot emit a manifest).
  - No tracked `evidence-schema2.json` (consistent with P1.2); CI `browser-evidence` job runs `npm run capture` before `npm test`.
  - No cross-file breakage.

### Passed

- `@cg-code-quality`: Review completed; P3.1/P3.2/P3.3 recorded above.
- `@cg-documentation`, `@cg-version-control`, `@cg-reproducibility`, `@cg-performance`, `@cg-architecture`, `@cg-data-quality`, `@cg-learnings-researcher`, `@cg-adversarial`: not dispatched (verify mode is light-only).

### ⚠️ Incomplete Reviews

- `@cg-testing` did not produce usable output. Consider re-running with a higher model tier, or invoke `@cg-testing` directly.

## Validation

- `node --check scripts/evidence/tests/manifest.test.js` — passed.
- `python -m pytest scripts/artifact_views/tests/test_evidence_schema2.py scripts/artifact_views/tests/test_themes.py` — 38 passed.
- `git diff --check` — passed.
- Node evidence suite (`npm test`) is CI-gated and requires a captured manifest; not runnable locally (offline, no node_modules).

Parsed 3 finding IDs. If count differs from total findings above, some IDs may be non-standard.
