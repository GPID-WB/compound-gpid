[pytest]
pythonpath = scripts
markers =
    integration: tests that exercise the real repository or external integration surface
    backend_posix: tests that exercise the POSIX secure publication backend
    backend_windows: tests that exercise the Windows secure publication backend
