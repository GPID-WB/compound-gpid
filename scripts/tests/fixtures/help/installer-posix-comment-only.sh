# Step 3: Create bin/ wrappers
# WRAPPER="$BIN_DIR/cg-index"
# cat > "$WRAPPER" <<'EOF'
# chmod +x "$WRAPPER"
# Step 4: Add bin/ to PATH via shell profile (idempotent)
printf 'Available command: cg-index\n'
