# Compound GPID — OpenCode Adapter

This file is generated from `.github/shared/target-mapping.json`.
It maps Compound GPID `/cg-*` commands to native OpenCode paths under `.opencode/`.

## Command Dispatch

`/cg-<name> [args...]` → `.opencode/commands/cg-<name>.md`

## Skills

Load skill files from `.opencode/skills/cg-skill-*/SKILL.md`.

## Agents

Agent specs are under `.opencode/agents/`.
