#!/bin/bash
# bin/cg-problems-summary -- summarize optional diagnostics JSON or text.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/../scripts/cg_summary.py" problems "$@"
