#!/bin/bash
# bin/cg-tree-summary -- summarize a bounded repository file tree.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/../scripts/cg_summary.py" tree "$@"
