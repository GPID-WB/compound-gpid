# Compound GPID — Codex Adapter

This file is generated from `.github/shared/target-mapping.json`.
It maps Compound GPID `/cg-*` commands to native Codex paths under `.agents/`.

## Command Dispatch

`/cg-<name> [args...]` → `.agents/commands/cg-<name>.md`

## Skills

Load skill files from `.agents/skills/cg-skill-*/SKILL.md`.

## Agents

Agent specs are under `.agents/agents/`.
